<nav class="grey darken-3">
    <div class="container">
        <div class="nav-wrapper">
            <a href="/stories" class="brand-logo center">Story Books</a>
            <a href="#" data-activates="main-menu" class="button-collapse show-on-large">
                <i class="fa fa-bars"></i>
            </a>
            <!-- RIGHT NAV -->
            <ul class="right">
                {{#if user}}
                    <li>
                        <a class="hide-on-med-and-down" href="/dashboard">Welcome {{user.firstName}}</a>
                    </li>

                    <li>
                        <a class="hide-on-small-only" href="/auth/logout"><i class="fas fa-sign-out-alt"> Logout</i></a>
                    </li>
                {{else}}
                    <li>
                        <a href="/stories"><i class = "fa fa-book"></i> Public Stories</a>
                    </li>
                {{/if}}
            </ul>
            <!-- SIDE MENU -->
            <ul class="side-nav" id="main-menu">
                    {{#unless user}}
                        <li>
                            <a class="red darken-1 btn" href="/auth/google"><i class="fab fa-google left"></i> Login With Google</a>
                        </li>
                        <li class="divider"></li>
                    {{/unless}}
                        <li>
                            <a href="/stories"><i class="fa fa-book"></i> Public Stories</a>
                        </li>
                            <li class="divider"></li>
                    {{#if user}}
                        <li>
                            <a href="/dashboard"><i class="fa fa-cog"></i> Dashboard</a>
                        </li>
                            <li class="divider"></li>
                        <li>
                            <a href="/stories/my"><i class="fa fa-book"></i> My Stories</a>
                        </li>
                            <li class="divider"></li>
                        <li>
                            <a href="/auth/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
                        </li>
                            <li class="divider"></li>
                    {{/if}}
            </ul>
        </div>
    </div>
</nav>